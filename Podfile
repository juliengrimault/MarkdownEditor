# Uncomment this line to define a global platform for your project
# platform :ios, "7.0"

target "MarkdownEditor" do

end

target "MarkdownEditorTests" do
  pod 'Expecta',     '~> 0.2.3'   # expecta matchers
  pod 'Specta',      '~> 0.1.11'  # specta bdd framework
end

